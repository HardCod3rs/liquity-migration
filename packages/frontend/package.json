{
  "name": "@liquity-migration/frontend",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react": "~17.0.0"
  }
}
